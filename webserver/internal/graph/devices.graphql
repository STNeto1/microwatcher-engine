type Device {
	id: ID!
	label: String!
	secret: String!
}

input CreateDevice {
	label: String!
}

extend type Query {
	devices: [Device!]!

	createDevice(input: CreateDevice!): Device!
	resetDeviceSecret(deviceID: ID!): Device!
}
